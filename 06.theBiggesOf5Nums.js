function solve(args) {
    var a = +args[0],
        b = +args[1],
        c = +args[2],
        d = +args[3],
        e = +args[4],
        max;

    max = Math.max(a, b, c, d, e);
    console.log(max);;
  

}

solve(['0.2', '9.572', '0.2', '5', '3']);